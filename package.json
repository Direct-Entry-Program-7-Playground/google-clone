{
  "name": "google-clone",
  "version": "1.2.0",
  "description": "a vanila html css google clone",
  "keywords": [
    "google",
    "clone"
  ],
  "license": "MIT",
  "author": "Manoj Randeni <randeni.ramr@gmail.com>",
  "scripts": {
    "clean": "rm -rf docs",
    "build": "npm run clean && parcel build index.html 404.html results.html --out-dir docs --public-url /google-clone/",
    "dev": "parcel watch index.html --open",
    "start": "npm run build && npm run dev"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  }
}
